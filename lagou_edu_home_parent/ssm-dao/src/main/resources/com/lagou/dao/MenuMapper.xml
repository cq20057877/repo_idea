<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lagou.dao.MenuMapper">

    <select id="findMenuByRoleId" parameterType="java.lang.Integer" resultType="java.lang.String">
        <!-- SELECT m.id FROM roles r ,role_menu_relation rmr , menu m WHERE r.id = rmr.role_id AND rmr.menu_id = m.id AND r.id = #{roleId} -->
        SELECT rml.menu_id FROM roles r , role_menu_relation rml WHERE r.id = rml.role_id AND r.id = #{roleId}
    </select>

    <select id="findAllMenu" resultType="com.lagou.domain.Menu">
        select * from menu
    </select>

    <select id="findMenuInfoById" parameterType="java.lang.Integer" resultType="com.lagou.domain.Menu">
        select * from menu where id = #{id}
    </select>

    <insert id="saveMenu" parameterType="com.lagou.domain.Menu">
        insert into menu values(null , #{parentId} , #{href} , #{icon}, #{name}, #{description}, #{orderNum}, #{shown}, #{level}, #{createdTime}, #{updatedTime}, #{createdBy}, #{updatedBy})
    </insert>

    <update id="updateMenu" parameterType="com.lagou.domain.Menu">
        UPDATE menu
        <trim prefix="SET" suffixOverrides=",">
            <if test="parentId != null and parentId != ''">
                parent_id = #{parentId},
            </if>
            <if test="href != null and href != ''">
                href = #{href},
            </if>
            <if test="icon != null and icon != ''">
                icon = #{icon},
            </if>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="description != null and description != ''">
                description = #{description},
            </if>
            <if test="orderNum != null and orderNum != ''">
                order_num = #{orderNum},
            </if>
            <if test="shown != null and shown != ''">
                shown = #{shown},
            </if>
            <if test="level != null and level != ''">
                level = #{level},
            </if>
            <if test="createdTime != null">
                created_time = #{createdTime},
            </if>
            <if test="updatedTime != null">
                updated_time = #{updatedTime},
            </if>
            <if test="createdBy != null">
                created_by = #{createdBy},
            </if>
            <if test="updatedBy != null">
                updated_by = #{updatedBy},
            </if>
        </trim>
        <where>
            <if test="id != null and id != ''">
            id = #{id}
            </if>
        </where>
    </update>
</mapper>
